# orca-relay runtime environment template
# Copy outside the repository, fill values there, then use with systemd EnvironmentFile.
# Do not commit filled secrets.

ORCA_RELAY_BIND=127.0.0.1:8080
ORCA_RELAY_TOKEN=replace-with-random-token
RUST_LOG=info
